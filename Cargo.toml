[package]
name = "rem"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.0", features = ["derive"] }
wasmtime = "45.0.0"
wat = "1.251.0"

[profile.release]
opt-level = 2
lto = "thin"
codegen-units = 16
panic = "unwind"
